<%= form_tag playlist_path(@playlist, pl_id: @playlist.id, user: current_user.user_name), method: :patch do %>
  <%= label_tag :name %>
  <%= text_field_tag :name %>
  <%= submit_tag "Create" %>
<% end %>
